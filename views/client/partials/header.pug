nav(class="navbar navbar-expand-lg navbar-light bg-white px-lg-3 py-lg-2 shadow-sm sticky-top")
  div(class="container-fluid")
    a(class="navbar-brand me-5 fw-bold fs-3 h-font" href="/") Khách sạn
    button(
      class="navbar-toggler shadow-none"
      aria-label="Toggle navigation" 
      aria-expanded="false" 
      aria-controls="navbarSupportedContent" 
      data-bs-target="#navbarSupportedContent" 
      data-bs-toggle="collapse" 
      type="button"
    )
      span(class="navbar-toggler-icon")
    div(class="collapse navbar-collapse" id="navbarSupportedContent")
      ul(class="navbar-nav me-auto mb-2 mb-lg-0")
        li(class="nav-item")
          a(
            class="nav-link me-2"
            href="/" 
            aria-current="page"
          )| Trang chủ
        li(class="nav-item")
          a(class="nav-link me-2"  href="/rooms") Phòng
        li(class="nav-item")
          a(class="nav-link me-2" href="/facilities") Dịch vụ
        li(class="nav-item")
          a(class="nav-link me-2" href="Contact") Liên hệ
      div(class="d-flex")
        if(user)
          .btn-group
            button.btn.btn-outline-dark.dropdown-toggle(
              aria-expanded="false" 
              data-bs-display="static" 
              data-bs-toggle="dropdown" 
              type="button"
              )
              span #{user.fullName}
            ul.dropdown-menu.dropdown-menu-lg-end
              li
                a.dropdown-item(href="profile.php") Profile
              li
                a.dropdown-item(href="bookings.php") Bookings
              li
                a.dropdown-item(href="/user/logout") Logout
        else 
          a(
            class="btn btn-outline-dark shadow-none me-lg-3 me-2"
            href="/user/login"
          ) Login
          a(
            class="btn btn-outline-dark shadow-none"
            href="/user/register"
          ) Register